[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tikworks"
authors = [{name = "Arda Kutlu", email = "ardakutlu@gmail.com"}]
requires-python = "> 3.10"
readme = "README.md"
dynamic = ["version", "description"]

[tool.setuptools]
packages = ["src/tikmaya"]